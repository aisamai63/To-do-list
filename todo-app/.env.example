### Copy this file to `.env` and fill in your MongoDB credentials.
# Do NOT commit your real `.env` to version control.

# Example using MongoDB Atlas (replace <password> and optionally the DB name):
# Note: URL-encode any special characters in the password (e.g. @, #, %)
MONGODB_URI=mongodb+srv://iabdo17_db_user:vGv2QpnwyIapXoE6@abdulsalam.boaz05z.mongodb.net/todo-app?retryWrites=true&w=majority

# Local fallback (if you run a local MongoDB):
# MONGODB_URI=mongodb://127.0.0.1:27017/todo-app

PORT=5000

# Quick tips:
# - Use the Atlas connection string provided in the Atlas UI (replace <password>).
# - If your password contains special characters, URL-encode it. Example:
#   raw: pa@ss/word
#   encoded: pa%40ss%2Fword